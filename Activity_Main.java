import java.awt.Color;

public class Activity_Main {
    public static void main(String[] args) {
       
/*
        // Création de point
        Point point = null;
        point = new Point(3,4); // Création d'un objet point1 basé sur le constructeur Point
        System.out.println(point);

        point.getTotalNumberOfPoint(); // Affichage du nombre de point total créé, penser à mettre la variable en "static" pour qu'elle puisse s'incrémenter sur tous les points
        
        // Création d'un rectangle
        Rectangle rectangle = null;
        rectangle = new Rectangle(20, 50, Color.black,point);
        System.out.println(rectangle);

        // Création d'une ellipse
        Ellipse ellipse = null;
        ellipse = new Ellipse(20, 50, Color.black,point);
        // System.out.println(ellipse);

        // Création d'un carré
        Square carre = null;
        carre = new Square(20,Color.black,point);
        // System.out.println(carre);

        // Création d'un cercle
        Circle cercle = null;
        cercle = new Circle(20,Color.black,point);
        // System.out.println(cercle); 
*/
        // Création de la fenêtre 
        // Window win = new Window("Paint",800,600);

        // Récupération de la liste de toutes les figures crées
            
        }
}
